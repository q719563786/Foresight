# 远见 v0.6 使用说明

1. 双击 `YuanJian.exe`，程序会直接打开“远见 · 外部认知大脑”桌面窗口，不再用浏览器承载主界面。首页“事件判断雷达”显示合并后的事件，不把重复链接当成多件事。
2. E1是单源线索，E2是两个独立域名，E3包含第一方/官方证据，E4要求更强的独立互证。点击卡片可看原始来源、因果链、反对证据和时间窗口。
3. “原始信息流”用于添加公开来源、关注词、暂停/恢复来源和立即刷新。抓取失败会显示真实原因，旧缓存仍保留。
4. 点击“立即运行认知”可手动聚类、研判、映射利益和产生提醒；按钮会立即显示进度、耗时和本次处理结果。不配置 API 时使用本地研判。
5. 对候选预测，必须人工选择 5%、10%、20%、35%、50%、65%、80%、90% 或95%后，才会进入正式预测账本。
6. “纠正本地判断”可以静音7天、降低重要度或标记误报；这些反馈只存本机。
7. “后台与AI设置”可查看后台状态、在独立程序中开启登录自动监控，并可选配置 OpenAI Responses API。模型编号和 API 密钥必须由用户自己填写；ChatGPT/Codex订阅不等于 API 额度。
8. API 密钥由 Windows 当前用户加密。界面不会回显密钥，只显示是否已配置。
9. “知识库”只读索引 Obsidian；不会修改原文。断网时历史缓存、本地研判、利益映射和预测账本仍可用。
10. 点窗口右上角关闭只会缩到系统托盘，互联网监控仍继续。单击托盘图标或托盘菜单“显示远见”可恢复窗口；托盘还可立即运行认知、暂停/恢复监控。
11. 真正停止程序请点界面“安全退出”或托盘“退出远见”。点击外部新闻或证据链接仍会使用系统浏览器打开。

运行数据位于 `%LOCALAPPDATA%\YuanJian`。不要把这个目录、数据库、备份或密钥文件上传 GitHub 或发给别人。
