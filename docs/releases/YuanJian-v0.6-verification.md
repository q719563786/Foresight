# YuanJian v0.6 发布验证

- 验证日期：2026-08-11
- 目标版本：0.6.0
- 发布形态：Windows pywebview 桌面窗口，关闭时缩入托盘
- 数据边界：私人数据库仍只保存在本机运行目录，不进入源码仓库或交付目录

## 自动化验证

- 全量测试：128 项，31.714 秒，结果 `OK`；`ResourceWarning` 按错误处理。
- 构建：PyInstaller 6.21.0、pywebview 6.2.1、pystray 0.19.5、Pillow 12.3.0，构建成功。
- 成品烟测：进程可启动并安全退出；只监听 `127.0.0.1`；首页状态 200；无远程脚本；默认进入认知视图；本地回退可用；第二实例退出码为 0。
- 私人数据库只读检查：`PRAGMA integrity_check = ok`，迁移版本为 `[1, 2, 3, 4]`。
- 数据规模只记录数量：预测 3、外部条目 991、事件簇 192、研判 258、个人利益影响 548。

## 真实桌面验收

- 正常启动直接显示标题为“远见 · 外部认知大脑”的桌面窗口，没有打开默认浏览器。
- 点击“立即运行认知”立即显示进行状态，并在结束后显示处理数量和耗时；按钮恢复可用。
- 点击窗口关闭按钮后窗口消失，原进程继续运行。
- 再次启动会唤醒同一个进程和同一个窗口，不产生第二个常驻实例。
- 托盘菜单已实测“显示远见”“立即运行认知”“暂停”“恢复后台监控”“退出远见”。
- 点击“退出远见”后进程和运行描述文件均消失。
- 当前用户登录自启项已完成创建、内容核对和移除测试，测试后未残留。

## 成品与回滚

- v0.6 交付目录：`YuanJianApp-v0.6`，共 164 个文件。
- v0.6 主程序大小：6,606,323 字节。
- v0.6 EXE SHA-256：`F547FC33ED0F8AFDFB329076147D765471DDF47C4AD1AA808EE390DC9FFB0C8A`。
- v0.5 EXE SHA-256：`3A7514BF7E2A267FFEF2DB47B47D3ACC2C402C01F46F1D8D1A222BC79C2DE9E8`。
- v0.5 目录继续保留，未被 v0.6 覆盖。
- 已按完整路径核验并停止遗留的 v0.3 进程；未按进程名称批量终止任何程序。

## 隐私与发布闸门

- Git 跟踪快照：72 个文件，隐私扫描 `blocked=0`、`findings=0`。
- v0.6 成品：隐私扫描 `blocked=0`、`findings=0`。
- v0.6 成品内数据库文件 0、环境文件 0、日志文件 0、验收截图 0。
- GitHub 仓库 `q719563786/yuanjian` 的可见性已复核为 `PRIVATE`，默认分支为 `main`。

## 结论

v0.6 满足桌面窗口、托盘常驻、单实例唤醒、认知反馈、回滚保留和隐私隔离要求，可以进入私有仓库集成阶段。
